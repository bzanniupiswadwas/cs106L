/*
#include "class1.h"

template <typename T, typename U>
Cser<T, U>::Cser(): name("Boven Ding"), job("C plus plus"), age(20) {}

template <typename T, typename U>
Cser<T, U>::Cser(T name, T job, U age) {
    set_name(name);
    set_job(job);
    set_age(age);
}

template <typename T, typename U>
T Cser<T, U>::getname() const {
    return name;
}

template <typename T, typename U>
T Cser<T, U>::getjob() const {
    return job;
}  

template <typename T, typename U>
U Cser<T, U>::getage() const {
    return age;
}

template <typename T, typename U>
void Cser<T, U>::set_name(T name) {
    this->name = name;
}

template <typename T, typename U>
void Cser<T, U>::set_job(T job) {
    this->job = job;
}   

template <typename T, typename U>
void Cser<T, U>::set_age(U age) {
    this->age = age;
}
*/
