/*
#pragma once
#include <string>

template <typename T, typename U>
class Cser {
public:
    Cser();
    Cser(T name, T job, U age);
    T getname() const;
    T getjob() const;
    U getage() const;

private:
    T name;
    T job;
    U age;
    void set_name(T name);
    void set_job(T job);
    void set_age(U age);
};
#include "class1.cpp"
*/